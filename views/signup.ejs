<html>
<meta name="viewport" content="width=device-width" />
<head>
    <title>Funds</title>
    <link rel="stylesheet" href="/lib/foundation/css/foundation.min.css"/>
    <link rel="stylesheet" href="/style/app.css"/>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.4.3/jquery.min.js"></script>
</head>
<body>
    <% include navbar %>
    <div class="row">
        <div class="small-12 large-offset-2 large-8 columns">
            <h2>Welcome to Funds!</h2>
            &nbsp;&nbsp;&nbsp;&nbsp;Funds is a mobile-friendly web application to help you keep track of your budget, and allow you to pinpoint where you spend most of your money! Track your spending from anywhere!
        </div>
    </div>
    <div class="row">
        <div class="small-12 large-offset-2 large-4 columns">
            <div class="login_box">
                <form method="POST">
                    <% if (locals.errors && errors.username) { %><label for="username" class="error"><%= errors.username.message %></label><% } %>
                    <input class="<% if (locals.errors && errors.username) { %>error<% } %>" name="username" type="text" placeholder="username" value="<%= slate.nullSafe(signup_user.username)%>"/>
                    
                    <% if (locals.errors && errors.password) { %><label for="password" class="error"><%= errors.password.message %></label><% } %>
                    <input class="<% if (locals.errors && errors.password) { %>error<% } %>" name="password" type="password" placeholder="password" value="<%= slate.nullSafe(signup_user.password)%>"/>
                    <input class="<% if (locals.errors && errors.password) { %>error<% } %>" name="confirm_password" type="password" placeholder="confirm_password" value="<%= slate.nullSafe(signup_user.confirm_password)%>"/>
                    
                    <% if (locals.errors && errors.email) { %><label for="email" class="error"><%= errors.email.message %></label><% } %>
                    <input class="<% if (locals.errors && errors.email) { %>error<% } %>" name="email" type="text" placeholder="email" value="<%= slate.nullSafe(signup_user.email)%>"/>
                    
                    <div class="row">
                        <div class="large-6 small-6 columns">
                            <input class="button expand success" type="submit" value="sign up" formaction="/signup" />
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
